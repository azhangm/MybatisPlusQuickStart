spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_db?userUnicode=true&characterEncoding=utf-8
    username: root
    password: root
  main:
    banner-mode: off
mybatis-plus:
  configuration:
    # ?? mybatis plus ??
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    banner: false